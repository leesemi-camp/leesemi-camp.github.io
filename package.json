{
  "name": "leesemi-camp.github.io",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "serve": "npx serve . -l 5173",
    "test": "playwright test"
  }
}