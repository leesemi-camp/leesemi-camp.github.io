{
  "name": "leesemi-camp.github.io",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "monocart-reporter": "^2",
    "playwright-monocart-coverage": "^1.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "serve": "npx serve . -l 5173",
    "build:boundaries": "node scripts/build-boundary-geojson.mjs",
    "export:hotspots": "node scripts/export-hotspots-public-json.mjs",
    "validate:hotspots": "node scripts/validate-hotspots-public-json.mjs",
    "test": "playwright test",
    "test:coverage": "playwright test --project=Chromium-Coverage"
  }
}
